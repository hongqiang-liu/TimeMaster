<Window x:Class="TimeMaster.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        Width="156" Height="48">
    <Window.Resources>
        <ResourceDictionary>
            <FontFamily x:Key="font-digital">pack://application:,,,/TimeMaster;component/Fonts/#Digital-7 Mono</FontFamily>
        </ResourceDictionary>
    </Window.Resources>
    <Border CornerRadius="10" Background="#88000000">
        <TextBlock x:Name="TimeText" 
                   Foreground="WhiteSmoke"
                   FontSize="36"
                   FontWeight="Light"
                   FontFamily="{StaticResource font-digital}"
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   Text="00:00:00">
            <!--<TextBlock.Effect>
                <DropShadowEffect Color="Black" Direction="315" 
                                 ShadowDepth="3" Opacity="0.5" BlurRadius="2"/>
            </TextBlock.Effect>-->
        </TextBlock>
    </Border>
</Window>
